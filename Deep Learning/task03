### 拟合相关

拟合： 在统计学中，拟合指的是你逼近目标函数的远近程度。
欠拟合： 模型没有很好地捕捉到数据特征，不能够很好地拟合数据。
过拟合： 模型把数据学习的太彻底，以至于把噪声数据的特征也学习到了，会导致在后期测试的时候不能够很好地识别数据，即不能正确的分类。

欠拟合解决方案：
1. 添加特征，有的时候出现欠拟合的原因是疑问特征不够
2. 添加多项式特征
3. 减少正则化参数


过拟合解决方案：
1. 由于数据不纯，需要清洗数据
2. 增大训练数据
3. 采用正则化的方法
4. 采用dropout的方法（利用在神经网络上）


